import { defineComponent } from "vue";

const Accordion = defineComponent({
    setup(props, ctx) {
        return () => (
            <div>

            </div>
        )
    },
})


export default Accordion